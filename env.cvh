#include "main.h"
#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>
#include <stdlib.h>

int getenv_(const char *name)
{
	int x;
	pid_t getpid = fork();

	if (getpid != 0)
	{
		exit(EXIT_SUCCESS);
	}
	else
	{
		for (x = 0; name[x] != '\0'; x++)
		{
			
		
	}
	return (0);
}
	
