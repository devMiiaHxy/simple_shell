#include "main.h"
#include <stdio.h>


int main(int argc, char *argv[])
{
	char* shell = " (SHELL) ";
	printf("%s", shell);

	char *content = "Hello";
	size_t x = 0;

	getline(&content, &x, stdin);
	printf("%s", content);

}
