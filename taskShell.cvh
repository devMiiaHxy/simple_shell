#include "main.h"
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
	size_t n = 0;
	char *shell_content;

	char *shell = " (MP_SHELL) ";
	printf("%s", shell);

	getline(&shell_content, &n, stdin);
	printf("%s", shell_content);

	return (0);
}

